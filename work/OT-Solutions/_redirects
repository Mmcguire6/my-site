# All legacy WebsiteBuilder URLs (/about-us, /services, /contact, /references,
# /kronos-expertise) are preserved as real pages on the new site, so no 301
# redirects are required at launch. This file is kept as a placeholder for
# future redirect entries.
